{% extends'base.html.twig' %}

{% block body %}
    {{ dump() }}

    {{ form(formArret) }}

    {% if arret is defined %}
        {{ dump(arret) }}
        {{ arret|first }}
    {% endif %}

{% endblock %}